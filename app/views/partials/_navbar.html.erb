<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand" href="/"><b>Photos</b></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Instructores
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <% User.all.each do |user| %>
              <%= link_to user.name, user.name, class:"dropdown-item" %>
            <% end %>
          </div>
        </li>
        <li class="nav-item">
          <% if signed_in? %>
            <div class="col-sm-2">
              <%= link_to "Nueva Foto", new_photo_path, class: "btn btn-outline-danger " %>
            </div>
          <% end %>
        </li>
      </ul>
      <ul class="navbar-nav">
        <% if signed_in? %>
          <li class="nav-item ">
            <%= link_to current_user.email , current_user.name, class: "nav-link"%>
          </li>
          <li class="nav-item ">
            <%= link_to "Salir" , destroy_user_session_path, method: :delete, class: "nav-link"%>
          </li>
        <% else %>
          <li class="nav-item ">
            <%= link_to "Ingresar" , new_user_session_path, class: "nav-link"%>
          </li>
          <li class="nav-item ">
            <%= link_to "Registrace" , new_user_registration_path, class: "nav-link"%>
          </li>
        <% end %>

    </div>
  </div>
</nav>
